version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: artist_alley
      POSTGRES_USER: artist
      POSTGRES_PASSWORD: alley123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
